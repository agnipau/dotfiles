#!/usr/bin/env bash
#
# Send a notification with the current date.

# shellcheck source=/dev/null
. "${HOME}/.local/share/icons-in-terminal/icons_bash.sh"

# shellcheck disable=SC2059,2154,1117
ico="$(printf "$fa_calendar\n")"

notify-send --expire-time=1000 "${ico} $(date '+%A %d %B')"
