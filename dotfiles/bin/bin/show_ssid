#!/usr/bin/env bash
#
# Send a notification with the SSID of the network I'm currently connected.

[[ "$(iwgetid -r)" ]] && notify-send --expire-time=3000 "$(iwgetid -r)"
[[ -z "$(iwgetid -r)" ]] && notify-send --expire-time=3000 "Not connected"
