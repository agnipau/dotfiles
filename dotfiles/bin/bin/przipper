#!/usr/bin/env bash
#
# Backup my projects (lightweight ones XD).

for project in ~/progetti/*; do
  if [[ "$project" != void-packages && "$project" != wallpapers ]]; then
    zip -9 -r "${project}.zip" "$project"
  fi
done
