; vim:ft=dosini

; Colors loaded from ~/.Xresources and generated by pywal
[colors]
background = ${xrdb:color0:#222}
foreground = ${xrdb:color7:#222}
primary = ${xrdb:color1:#222}
secondary = ${xrdb:color2:#222}
alert = ${xrdb:color3:#222}
empty = ${xrdb:color8:#222}

[bar/bar]
width = 100%
height = 40
radius = 0
fixed-center = false
bottom = true
background = ${colors.background}
foreground = ${colors.foreground}
padding-left = 3
padding-right = 3
module-margin-left = 1
module-margin-right = 2
font-0 = Iosevka Term:size=12:antialias=true:autohint=true;1
font-1 = FontAwesome:size=12:antialias=true:autohint=true;1
modules-left = openbox_wms
modules-center = 
modules-right = brightness volume network battery date
locale = it_IT.UTF-8
;cursor-click = pointer
;cursor-scroll = ns-resize

[module/openbox_wms]
type = internal/xworkspaces
pin-workspaces = true
enable-click = true
enable-scroll = true
icon-0 = uno;"  "
icon-1 = due;"  "
icon-2 = tre;"  "
icon-3 = quattro;"  "
icon-4 = cinque;"  "
icon-5 = sei;"  "
format = <label-state>
label-urgent = %icon%
label-urgent-background = ${colors.background}
label-urgent-foreground = ${colors.alert}
label-active = %icon%
label-active-background = ${colors.background}
label-active-foreground = ${colors.primary}
label-occupied = %icon%
label-occupied-background = ${colors.background}
label-occupied-foreground = ${colors.foreground}
label-empty = %icon%
label-empty-background = ${colors.background}
label-empty-foreground = ${colors.empty}

[module/brightness]
type = custom/script
exec = ~/bin/lum --current
interval = 1
format = <label>
format-background = ${colors.background}
format-foreground = ${colors.empty}
label = %output% 
scroll-up = ~/bin/lum --inc 5
scroll-down = ~/bin/lum --dec 5
click-left = redshift -PO 4500
click-right = redshift -x

[module/volume]
type = internal/volume
mapped = true
format-volume = <label-volume> <ramp-volume>
ramp-volume-0 = 
ramp-volume-0-background = ${colors.background}
ramp-volume-0-foreground = ${colors.empty}
ramp-volume-1 = 
ramp-volume-1-background = ${colors.background}
ramp-volume-1-foreground = ${colors.empty}
ramp-volume-2 = 
ramp-volume-2-background = ${colors.background} 
ramp-volume-2-foreground = ${colors.empty} 
label-muted =  muted
label-muted-background = ${colors.background}
label-muted-foreground = ${colors.empty}

[module/network]
type = custom/script
exec = ~/bin/net
interval = 1
format = <label>
format-background = ${colors.background}
format-foreground = ${colors.empty}
label = %output% 
click-left = st -g 50x22 -e nmtui

[module/battery]
type = internal/battery
battery = BAT0
adapter = AC0
full-at = 100
format-charging = <label-charging> <ramp-capacity>
format-discharging = <label-discharging> <ramp-capacity>
format-background = ${colors.background}
format-foreground = ${colors.empty}
label-charging = %percentage%% 
label-discharging = %percentage%%
ramp-capacity-0 =  
ramp-capacity-0-background = ${colors.background}
ramp-capacity-0-foreground = ${colors.empty}
ramp-capacity-1 =  
ramp-capacity-1-background = ${colors.background}
ramp-capacity-1-foreground = ${colors.empty}
ramp-capacity-2 =  
ramp-capacity-2-background = ${colors.background}
ramp-capacity-2-foreground = ${colors.empty}
ramp-capacity-3 =  
ramp-capacity-3-background = ${colors.background}
ramp-capacity-3-foreground = ${colors.empty}
ramp-capacity-4 =  
ramp-capacity-4-background = ${colors.background}
ramp-capacity-4-foreground = ${colors.empty}

[module/date]
type = internal/date
interval = 1
date = "%a %d"
date-alt = "%a %d"
time = %H:%M
time-alt = %H:%M:%S
format = <label> 
format-background = ${colors.background}
format-foreground = ${colors.empty}
label = %date% %time%
label-foreground = ${colors.empty}
