#!/usr/bin/env bash
#
# Send a notification with the SSID of the network I'm currently connected.

test "$(iwgetid -r)" && notify-send "$(iwgetid -r)"
test -z "$(iwgetid -r)" && notify-send "Not connected"
